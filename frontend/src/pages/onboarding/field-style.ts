/** Shared premium field treatment for onboarding inputs/selects: taller, softer border, heavier fill, quieter focus ring. */
export const FIELD_SIZE =
  'h-14 rounded-xl border-border-subtle bg-surface-raised text-body focus:border-accent/50 focus:ring-[3px] focus:ring-accent/15'

/** Lighter label weight/color so the input carries the visual weight, not the label. */
export const LABEL_STYLE = 'font-normal text-muted-foreground'
