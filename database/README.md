# database/

This is where Person C's Supabase schema and save/load helper functions live: the `boards`
table definition and the `saveBoard` / `loadBoard` / `updateBoardTasks` functions.

No code here yet — see [`../docs/PERSON_C_DATABASE.md`](../docs/PERSON_C_DATABASE.md) for
scope, responsibilities, and the checklist for this role.
