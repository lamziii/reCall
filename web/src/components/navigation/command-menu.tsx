// "Command menu" is the same pattern as CommandPalette — one Cmd/Ctrl+K
// overlay, named to match whichever term a given page's copy uses.
export { CommandPalette as CommandMenu } from './command-palette'
export type { CommandPaletteProps as CommandMenuProps } from './command-palette'
