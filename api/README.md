# api/

This is where Person B's Supabase Edge Function(s) live: `extract-tasks`, which calls the
Claude API to turn raw notes into structured tasks.

No code here yet — see [`../docs/PERSON_B_API.md`](../docs/PERSON_B_API.md) for scope,
responsibilities, and the checklist for this role.
